/**
 * Central type definitions — pure domain types, no framework dependencies.
 * players, matches, rankings, seasons, Elo ratings, streaks, and leaderboard snapshots
 */

// Add domain-specific types here
export {}
